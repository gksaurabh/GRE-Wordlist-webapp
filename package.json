{
  "dependencies": {
    "express": "^4.18.1",
    "mongodb": "^4.6.0",
    "mongoose": "^6.3.3",
    "nodemon": "^2.0.16"
  },
  "scripts": {
    "start": "node server.js",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "build": "cd gre-flashcart-app && npm run build",
    "install-client": "cd gre-flashcart-app && npm install",
    "heroku-postbuild": "npm run install-client && npm run build"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0"
  }
}
